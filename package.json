{
  "name": "range",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "pretest": "npm install",
    "test": "./node_modules/.bin/mocha test"
  },
  "dependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.1.3",
    "mocha-junit-reporter": "^1.21.0",
    "mocha-multi-reporters": "^1.1.7"
  }
}
